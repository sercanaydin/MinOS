# Limine önyükleyici yapılandırması (UEFI + BIOS).
# Çekirdeği multiboot1 protokolüyle, doğrusal bir framebuffer ile başlatır.

timeout: 0

/MinOS
    protocol: multiboot1
    path: boot():/boot/rustos.bin
    resolution: 1024x768x32
